# B 階段:部署 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 讓這套系統跑在容器裡、資料在 PostgreSQL、對外有一個永遠不變的網址,當掉會自己起來。

**Architecture:** 一份 `compose.yaml` 帶三個服務 —— `app`(FastAPI,開機自動跑 Alembic)、`db`(PostgreSQL,named volume)、`tailscale`(Funnel 提供固定 HTTPS 入口)。容器化與「跑在哪台機器」刻意解耦:compose 起得來的地方就跑得起來,之後搬上任何一台 Linux 機器設定檔一行不用改。

**Tech Stack:** Docker Compose · PostgreSQL 17 · Tailscale Funnel · FastAPI · SQLAlchemy 2 · Alembic · pytest

**Spec:** [docs/superpowers/specs/2026-09-22-deploy-design.md](../specs/2026-09-22-deploy-design.md)

**Branch:** `feat/deploy`(已建立,spec 已 commit 在上面)

> **`helpdesk.tail9a2f.ts.net` 是佔位用的假網址。** 真正的 FQDN 要到 Task 6 Step 8 才拿得到 —— 從那裡開始,底下所有指令裡的這串都要換成當時記下來的值。

## Global Constraints

每一個 task 的要求都隱含包含這一節。

1. **PostgreSQL 主版本釘死 `postgres:17-alpine`。** 不准用 `latest` —— 資料目錄格式跟主版本綁定,映像跳版會對著既有 volume 直接啟動失敗,而錯誤訊息講的是資料目錄版本不符,不會說「你換了映像」。
2. **機密走 `.env`,非機密直接寫 `compose.yaml`。** 機密只有四個:`POSTGRES_PASSWORD`、`TS_AUTHKEY`、`FERNET_KEY`、`OPENROUTER_API_KEY`。
3. **`.env`、`.venv/`、`*.db`、`.git/` 一律不得進入映像。**
4. **對外埠只綁 `127.0.0.1`。** 注意這是指 compose 的 port publish(`127.0.0.1:8000:8000`);**容器內部的 uvicorn 必須聽 `0.0.0.0`**,否則 port publish 轉不進去。這兩件事不一樣,不要互相套用。
5. **密文欄位一律照搬不解密。**
6. **時間一律 UTC。** 從 SQLite 讀出的 naive datetime 必須明確標記 UTC 才能寫入 PostgreSQL 的 `timestamptz`。
7. **`/health` 不得回傳任何設定值。** 連線字串裡有資料庫密碼,而這個端點是公開的。
8. **腳本不自己建表。** 建表是 Alembic 的職責。
9. **指令一律用 PowerShell 語法。** `VAR=值 指令` 是 bash 的前綴寫法,在 PowerShell 會直接報錯。Python 一律用 `.\.venv\Scripts\python.exe`。
10. **所有從外部進來的字串設定值都要 `strip()`**(既有 Global Constraint 3:含換行的 HTTP 標頭會被整個丟掉,上游只回「Missing Authentication header」)。

---

### Task 1: 讓整套測試跑在 PostgreSQL 上,並把 psycopg 移到主依賴

**Files:**
- Modify: `pyproject.toml`
- Modify: `tests/test_database.py`
- Modify: `.github/workflows/ci.yml`

**Interfaces:**
- Consumes: 無
- Produces: 主依賴含 `psycopg[binary]>=3.2`(Task 3 的映像靠它連得上資料庫);CI 多一個 `test-postgres` job

這個 task 必須第一個做:`psycopg` 現在掛在 `dev` extra 裡,映像裝了主依賴卻連不上 PostgreSQL,而且是**到第一次連線才炸**。後面每一個 task 都踩在它上面。

- [ ] **Step 1: 起一個臨時 PostgreSQL 容器**

```powershell
docker run --rm -d --name helpdesk-pgtest -e POSTGRES_PASSWORD=test -e POSTGRES_DB=helpdesk_test -p 55432:5432 postgres:17-alpine
```

用 55432 而不是 5432,避免撞到機器上已經在跑的 PostgreSQL。

- [ ] **Step 2: 用 PostgreSQL 跑現有測試,確認會失敗**

```powershell
$env:DATABASE_URL = "postgresql+psycopg://postgres:test@localhost:55432/helpdesk_test"
.\.venv\Scripts\python.exe -m pytest -q
```

Expected: `tests/test_database.py::test_pragma_is_actually_on` FAILED —— `PRAGMA foreign_keys` 是 SQLite 專屬語法,PostgreSQL 看不懂。

**其他測試如果也紅了,把失敗清單記下來,在 Step 3 一起處理。** 預期只有這一個,但這是預期不是保證。

- [ ] **Step 3: 把 SQLite 專屬的測試標成只在 SQLite 跑**

在 `tests/test_database.py` 最上方的 import 區加 `os`,並在 `test_pragma_is_actually_on` 上加標記:

```python
import os
from unittest.mock import patch

import pytest
from sqlalchemy import Column, ForeignKey, Integer, String, text
from sqlalchemy.exc import IntegrityError
from sqlalchemy.orm import Session

from app.database import Base, session_scope

# PRAGMA 是 SQLite 專屬語法。PostgreSQL 的外鍵本來就一定是開著的,
# 沒有對應的「忘記打開」風險 —— 所以這條在 PG 上不是失敗,是不適用。
SQLITE_ONLY = pytest.mark.skipif(
    not os.environ.get("DATABASE_URL", "sqlite://").startswith("sqlite"),
    reason="PRAGMA 是 SQLite 專屬語法",
)
```

然後:

```python
@SQLITE_ONLY
def test_pragma_is_actually_on(tmp_engine):
    with tmp_engine.connect() as conn:
        assert conn.execute(text("PRAGMA foreign_keys")).scalar() == 1
```

`test_sqlite_enforces_foreign_keys` **不要加標記** —— 它在 PostgreSQL 上一樣該過(PG 本來就檢查外鍵),讓它兩邊都跑是有價值的。

- [ ] **Step 4: 重跑,確認 PostgreSQL 全綠**

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

Expected: 全部 passed,`test_pragma_is_actually_on` 顯示 skipped。

- [ ] **Step 5: 確認 SQLite 那組沒被弄壞**

```powershell
Remove-Item Env:DATABASE_URL
.\.venv\Scripts\python.exe -m pytest -q
```

Expected: 142 passed(`test_pragma_is_actually_on` 這次要真的跑,不是 skipped)。

- [ ] **Step 6: `psycopg` 移到主依賴**

`pyproject.toml`:

```toml
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "sqlalchemy>=2.0",
    "alembic>=1.14",
    "pydantic-settings>=2.6",
    "httpx>=0.28",
    "jinja2>=3.1",
    "cryptography>=43",
    "PyYAML>=6.0",
    # 正式環境跑 PostgreSQL。放在 dev extra 裡的話,映像裝了主依賴卻
    # 連不上資料庫,而且是到第一次連線才炸 —— 那時錯誤訊息講的是
    # 「找不到 driver」,指不到「依賴宣告在錯的地方」。
    # [binary] 有 aarch64 的 wheel,之後搬 ARM 機器不用改。
    "psycopg[binary]>=3.2",
]

[project.optional-dependencies]
dev = ["pytest>=8.3", "pytest-asyncio>=0.24"]
```

- [ ] **Step 7: 驗證乾淨安裝真的帶得到 psycopg**

在暫存目錄開一個乾淨 venv(不要動專案的 `.venv`):

```powershell
$tmp = Join-Path $env:TEMP "helpdesk-cleanvenv"
Remove-Item -Recurse -Force $tmp -ErrorAction SilentlyContinue
py -3.13 -m venv $tmp
& "$tmp\Scripts\python.exe" -m pip install -q .
& "$tmp\Scripts\python.exe" -c "import psycopg; print('psycopg', psycopg.__version__)"
```

Expected: 印出版本號,沒有 ImportError。

```powershell
Remove-Item -Recurse -Force $tmp
```

- [ ] **Step 8: CI 加 PostgreSQL job**

在 `.github/workflows/ci.yml` 的 `jobs:` 底下,`test:` 之後加:

```yaml
  test-postgres:
    # app/database.py 自己的註解就寫了:「本機測試全過,上了 PostgreSQL
    # 才發現一堆孤兒資料」。那句話當初是預防性的,現在真的要上 PostgreSQL 了,
    # 這個 job 就是那句註解的保險。只跑一個 Python 版本 —— 這裡要驗的是
    # 資料庫方言,不是 Python 版本差異,那個上面那個 job 已經在驗了。
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_PASSWORD: test
          POSTGRES_DB: helpdesk_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip

      - name: 安裝相依
        run: pip install -e ".[dev]"

      - name: 跑測試(PostgreSQL)
        env:
          DATABASE_URL: postgresql+psycopg://postgres:test@localhost:5432/helpdesk_test
        run: pytest -q
```

- [ ] **Step 9: 收掉臨時容器並 commit**

```powershell
docker stop helpdesk-pgtest
git add pyproject.toml tests/test_database.py .github/workflows/ci.yml
git commit -m "ci(db): psycopg 移到主依賴,測試加跑一輪 PostgreSQL

psycopg 掛在 dev extra 裡的話,正式映像裝了主依賴卻連不上資料庫,
而且是到第一次連線才炸 —— 錯誤訊息講的是找不到 driver,
指不到真正原因是依賴宣告在錯的地方。

PRAGMA foreign_keys 是 SQLite 專屬語法,在 PG 上標成 skip。
test_sqlite_enforces_foreign_keys 刻意不標 —— 它在兩種資料庫上
都該過,讓它兩邊都跑才有意義。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: `/health` 加資料庫檢查

**Files:**
- Modify: `app/main.py`
- Test: `tests/test_health.py`(新建)

**Interfaces:**
- Consumes: `app.database.engine`
- Produces: `app.main._database_ok() -> bool`;`GET /health` 回 `{"status", "version", "database"}`,資料庫不通時 HTTP 503。Task 4 的 compose healthcheck 靠這個 503。

- [ ] **Step 1: 寫失敗的測試**

新建 `tests/test_health.py`:

```python
"""/health 的測試。

這個端點有兩個讀者:compose 的 healthcheck,以及 demo 前 curl 一下的人。
第二個才是重點 —— 只證明行程活著的話,資料庫斷了它照樣回 ok,
而那正是最需要它講實話的時候。
"""

from fastapi.testclient import TestClient

import app.main as main_module
from app.database import Base, engine
from app.main import app

client = TestClient(app)


def test_health_is_ok_when_the_database_answers():
    Base.metadata.create_all(engine)
    body = client.get("/health").json()
    assert body["status"] == "ok"
    assert body["database"] == "ok"


def test_health_reports_fail_when_the_database_is_down(monkeypatch):
    """資料庫斷了要說出來。回 ok 的話,compose 的 healthcheck 跟人都會
    以為沒事,而真正的症狀(客人沒收到回覆)出現在完全別的地方。"""

    def boom():
        raise RuntimeError("connection refused")

    monkeypatch.setattr(main_module, "_database_ok", boom)
    r = client.get("/health")
    assert r.status_code == 503
    assert r.json()["database"] == "fail"


def test_health_never_leaks_the_connection_string(monkeypatch):
    """這個端點是公開的,而連線字串裡有資料庫密碼。
    例外訊息尤其危險 —— SQLAlchemy 的連線錯誤常常把整串 URL 印出來。"""

    def boom():
        raise RuntimeError(
            "could not connect to postgresql+psycopg://helpdesk:hunter2@db:5432/helpdesk")

    monkeypatch.setattr(main_module, "_database_ok", boom)
    text = client.get("/health").text
    assert "hunter2" not in text
    assert "psycopg" not in text
```

- [ ] **Step 2: 跑測試確認失敗**

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_health.py -v
```

Expected: FAIL —— `KeyError: 'database'`(現在的 `/health` 沒有這個欄位),以及 `AttributeError: module 'app.main' has no attribute '_database_ok'`。

- [ ] **Step 3: 改 `app/main.py`**

整個檔案改成:

```python
"""FastAPI 進入點。"""

from __future__ import annotations

import logging

from fastapi import FastAPI, Response
from sqlalchemy import text

from app.database import engine
from app.routers import webhook

logging.basicConfig(level=logging.INFO,
                    format="%(asctime)s %(levelname)s %(name)s | %(message)s")

logger = logging.getLogger(__name__)

app = FastAPI(title="AI 智慧客服(萬用)")
app.include_router(webhook.router)


def _database_ok() -> bool:
    """真的去問資料庫一句話,不是只看設定有沒有填。

    分成獨立函式是為了測試能換掉它 —— 要驗「資料庫斷了會怎樣」,
    總不能真的去把資料庫關掉。
    """
    with engine.connect() as conn:
        conn.execute(text("SELECT 1"))
    return True


@app.get("/health")
def health(response: Response):
    # 不回傳任何設定值 —— 這個端點是公開的。例外訊息尤其不能往外送:
    # SQLAlchemy 的連線錯誤常常把整串連線字串印出來,而那裡面有密碼。
    try:
        _database_ok()
        database = "ok"
    except Exception:
        logger.exception("健康檢查:資料庫連不上")
        database = "fail"

    if database != "ok":
        response.status_code = 503

    return {
        "status": "ok" if database == "ok" else "degraded",
        "version": "0.1.0",
        "database": database,
    }
```

- [ ] **Step 4: 跑測試確認通過**

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_health.py -v
```

Expected: 3 passed

- [ ] **Step 5: 跑全套,確認沒弄壞別的**

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

Expected: 145 passed

- [ ] **Step 6: Commit**

```powershell
git add app/main.py tests/test_health.py
git commit -m "feat(health): /health 真的去問資料庫一句話

原本只證明行程活著。資料庫斷了它照樣回 ok,而那正是最需要它
講實話的時候 —— 症狀會跑到別的地方出現(客人沒收到回覆),
查起來完全指不到這裡。

不通時回 503,compose 的 healthcheck 才看得出來。
例外訊息一律不往外送:SQLAlchemy 的連線錯誤常常把整串連線字串
印出來,而那裡面有資料庫密碼,這個端點是公開的。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: `Dockerfile` 與 `.dockerignore`

**Files:**
- Create: `Dockerfile`
- Create: `.dockerignore`
- Create: `docker-entrypoint.sh`

**Interfaces:**
- Consumes: Task 1 的主依賴(含 psycopg)
- Produces: 映像 `helpdesk-app`,entrypoint 先 `alembic upgrade head` 再起 uvicorn(聽 `0.0.0.0:8000`)。Task 4 的 compose 用 `build: .` 建它。

- [ ] **Step 1: 寫 `.dockerignore`**

```
.git/
.github/
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# 金鑰絕對不能進映像。COPY 把它烤進某一層之後,就算下一層 rm 掉,
# 它還留在前一層的歷史裡 —— 誰 docker history 都挖得出來。
.env
.env.*
!.env.example

# 本機 SQLite 資料庫。它是搬遷的「來源」,不是映像的一部分。
*.db
*.db-journal

# 文件與測試不進正式映像
docs/
tests/
.superpowers/
.idea/
```

- [ ] **Step 2: 寫 `docker-entrypoint.sh`**

```sh
#!/bin/sh
set -e

# 建表與改表一律走 Alembic。只有一個 app 容器,不會有兩個同時跑 migration。
echo "== alembic upgrade head =="
alembic upgrade head

# 容器內部必須聽 0.0.0.0,否則 compose 的 port publish 轉不進來。
# 「只綁 127.0.0.1」是 compose 那一層的事(見 Global Constraint 4),
# 不是這一層。
echo "== uvicorn =="
exec uvicorn app.main:app --host 0.0.0.0 --port 8000
```

- [ ] **Step 3: 寫 `Dockerfile`**

```dockerfile
# syntax=docker/dockerfile:1

FROM python:3.13-slim

# psycopg[binary] 自帶 libpq,不必裝 postgresql-client。
# curl 留著是因為 compose 的 healthcheck 要用它打 /health。
RUN apt-get update \
    && apt-get install -y --no-install-recommends curl \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# 先只複製宣告相依的東西再裝。原始碼改動不會讓 pip 那一層失效,
# 重 build 從幾分鐘變幾秒。
COPY pyproject.toml ./
COPY app ./app
RUN pip install --no-cache-dir .

# 執行期才需要、但不是 Python 套件的一部分
# (pyproject 的 include 只有 app*,理由見那個檔案的註解)
COPY alembic ./alembic
COPY alembic.ini ./
COPY industries ./industries
COPY prompts ./prompts
COPY docker-entrypoint.sh ./
RUN chmod +x docker-entrypoint.sh

# 不緩衝輸出。緩衝的話容器 log 會一片空白直到緩衝區滿,
# 而「看不到 log」在排查時跟「服務沒起來」長得一模一樣。
ENV PYTHONUNBUFFERED=1

EXPOSE 8000
ENTRYPOINT ["./docker-entrypoint.sh"]
```

- [ ] **Step 4: Build**

```powershell
docker build -t helpdesk-app .
```

Expected: build 成功。

- [ ] **Step 5: 驗證映像裡裝得起來、import 得進去**

```powershell
docker run --rm --entrypoint python helpdesk-app -c "import psycopg, app.main; print('ok')"
```

Expected: `ok`

- [ ] **Step 6: 驗證 `.env` 沒有進映像(這一步不能跳)**

```powershell
docker run --rm --entrypoint sh helpdesk-app -c "find / -name '.env' -not -path '/proc/*' 2>/dev/null; echo '--- 上面應該是空的 ---'"
```

Expected: `---` 那行之前沒有任何輸出。

有輸出就代表 `.dockerignore` 沒生效,**停下來修好再往下**。金鑰進了映像層之後刪不掉。

- [ ] **Step 7: Commit**

```powershell
git add Dockerfile .dockerignore docker-entrypoint.sh
git commit -m "feat(docker): 映像與 entrypoint

entrypoint 開機先 alembic upgrade head 再起 uvicorn。建表一律走
Alembic,不讓第二個地方也能建 —— 同一件事兩個真相來源,總有一天
會不一致,而且會在「某個欄位莫名不存在」的時候才發現。

.dockerignore 擋掉 .env:COPY 把它烤進某一層之後,下一層 rm 掉也
沒用,它還在前一層的歷史裡,docker history 挖得出來。

容器內部聽 0.0.0.0 是必要的,不然 compose 的 port publish 轉不進去。
「只綁 127.0.0.1」是 compose 那一層的事。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: `compose.yaml` 的 `db` 與 `app`

**Files:**
- Create: `compose.yaml`
- Modify: `.env.example`
- Modify: `.env`(本機,不進版控)

**Interfaces:**
- Consumes: Task 3 的 `Dockerfile`、Task 2 的 `/health`
- Produces: `db` 與 `app` 兩個服務、`pgdata` volume、`http://127.0.0.1:8000`。Task 5 搬遷、Task 6 的 tailscale 都接在這上面。

這個 task 先不含 `tailscale` —— 先讓「服務 + 資料庫」這條通了,再加對外那一段。三個一起上的話,出問題分不清是哪一層。

- [ ] **Step 1: `.env.example` 加新的變數**

在檔案最上方的 `DATABASE_URL` 區塊改成:

```bash
# SQLite(筆電開發):sqlite:///./helpdesk.db
# PostgreSQL(compose):由 compose.yaml 組出來指向 db 服務,不必在這裡填。
#   只有「在主機上直接連容器裡的資料庫」時才要 —— 例如跑搬遷腳本:
#   postgresql+psycopg://helpdesk:<密碼>@localhost:5432/helpdesk
DATABASE_URL=sqlite:///./helpdesk.db
```

在檔案最後加:

```bash
# --- compose 用 ---

# PostgreSQL 的密碼。自己想一個,不必是人記得住的。
POSTGRES_PASSWORD=

# Tailscale auth key(Task 6 才會用到)。
# 必須是帶 tag:helpdesk 的 key —— 標記過的節點不適用金鑰到期,
# 沒打 tag 的話 180 天後某一天服務會自己斷線,而且你不會知道為什麼。
TS_AUTHKEY=
```

- [ ] **Step 2: 本機 `.env` 補上 `POSTGRES_PASSWORD`**

```powershell
Add-Content -Path .env -Value "POSTGRES_PASSWORD=$([guid]::NewGuid().ToString('N'))" -Encoding utf8
Select-String -Path .env -Pattern "POSTGRES_PASSWORD"
```

Expected: 印出一行 `POSTGRES_PASSWORD=<32 位十六進位>`

- [ ] **Step 3: 寫 `compose.yaml`**

```yaml
name: helpdesk

services:
  db:
    # 主版本釘死,不准寫 latest。資料目錄格式跟主版本綁定 —— 映像跳版會
    # 對著既有的 pgdata volume 直接啟動失敗,而錯誤訊息講的是資料目錄
    # 版本不符,不會說「你換了映像」。要升級請有意識地跑 pg_upgrade。
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: helpdesk
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?請在 .env 設 POSTGRES_PASSWORD}
      POSTGRES_DB: helpdesk
    ports:
      # 只給 127.0.0.1。Task 5 的搬遷腳本跑在主機上(來源那個 SQLite 檔
      # 被 .dockerignore 擋掉了,不在映像裡),沒有這行它連不進來。
      # 平常用 psql 看一眼資料也靠它。絕對不要改成 0.0.0.0 ——
      # 那等於把資料庫端口開給同網段的所有人。
      - "127.0.0.1:5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U helpdesk -d helpdesk"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  app:
    build: .
    depends_on:
      # 沒有這個,開機第一件事 alembic upgrade head 會在 PostgreSQL 還沒
      # ready 時失敗。restart policy 會一直重試直到成功,但 log 會塞滿
      # 指不到真正原因的錯誤。
      db:
        condition: service_healthy
    environment:
      # 容器裡連的是服務名 db,不是 localhost
      DATABASE_URL: postgresql+psycopg://helpdesk:${POSTGRES_PASSWORD:?}@db:5432/helpdesk
      FERNET_KEY: ${FERNET_KEY:?請在 .env 設 FERNET_KEY}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:?請在 .env 設 OPENROUTER_API_KEY}
      OPENROUTER_MODEL: ${OPENROUTER_MODEL:-nvidia/nemotron-3-super-120b-a12b:free}
      OPENROUTER_FALLBACK_MODEL: ${OPENROUTER_FALLBACK_MODEL:-openrouter/free}
    ports:
      # 只綁 127.0.0.1。綁 0.0.0.0 的話,同網段的人(會議室 Wi-Fi)
      # 直接打得到 webhook 端點。容器內部仍然聽 0.0.0.0 —— 那是必要的,
      # 不然這個 port publish 轉不進去。兩件事不一樣。
      - "127.0.0.1:8000:8000"
    healthcheck:
      # 注意:healthcheck 不會重啟容器(那是 Swarm 的行為)。它的用途是
      # 讓 docker compose ps 看得出狀態,以及給別的服務 depends_on 等。
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    restart: unless-stopped

volumes:
  pgdata:
```

`${VAR:?訊息}` 是刻意的:變數沒設時 compose 直接拒絕啟動並印出那句話,而不是帶著空字串起來、到連線時才給一個看不懂的錯。

- [ ] **Step 4: 起起來**

```powershell
docker compose up -d --build
docker compose ps
```

Expected: `db` 顯示 `healthy`,`app` 顯示 `running`(30 秒的 `start_period` 過後才會變 `healthy`)。

- [ ] **Step 5: 驗收標準 1 與 2**

```powershell
docker compose logs app --tail 30
curl.exe -s http://127.0.0.1:8000/health
```

Expected:
- log 裡看得到 `== alembic upgrade head ==` 與 Alembic 的 migration 訊息,然後是 uvicorn 的啟動行
- curl 回 `{"status":"ok","version":"0.1.0","database":"ok"}`

- [ ] **Step 6: 確認表真的建出來了**

```powershell
docker compose exec db psql -U helpdesk -d helpdesk -c "\dt"
```

Expected: 列出 `alembic_version`、`chat_histories`、`companies`、`knowledge_documents`、`users`。

- [ ] **Step 7: Commit**

```powershell
git add compose.yaml .env.example
git commit -m "feat(docker): compose 的 app 與 db 兩個服務

先不含 tailscale —— 三層一起上,出問題分不清是哪一層。

db 用 pg_isready 做 healthcheck,app 用 depends_on service_healthy 等它。
沒有這個,開機第一件事 alembic upgrade head 會在 PostgreSQL 還沒 ready
時失敗,restart policy 會一直重試到成功,但 log 塞滿指不到真正原因的錯誤。

port 只綁 127.0.0.1:會議室 Wi-Fi 上同網段的人不該打得到 webhook 端點。

環境變數用 \${VAR:?訊息}:沒設時 compose 直接拒絕啟動並說哪個沒填,
而不是帶著空字串起來、到連線時才給一個看不懂的錯。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: SQLite → PostgreSQL 搬遷腳本

**Files:**
- Create: `scripts/migrate_sqlite_to_postgres.py`
- Test: `tests/test_migrate.py`(新建)

**Interfaces:**
- Consumes: Task 4 的 `db` 服務(表必須已由 Alembic 建好)
- Produces: `as_utc(value)`、`copy_all(source_url, target_url, *, dry_run=False, force=False) -> dict[str, int]`

- [ ] **Step 1: 寫失敗的測試**

新建 `tests/test_migrate.py`:

```python
"""搬遷腳本的測試。

scripts/ 不是套件,所以用檔案路徑載入(跟 test_dev_script.py 同一套做法)。

真正要守的是時區那一條:它壞掉的時候不會報錯,只是整批時間差八小時,
而且要等到有人去看對話紀錄的時間戳才會發現。
"""

import importlib.util
from datetime import datetime, timezone
from pathlib import Path

import pytest
from sqlalchemy import create_engine, select

from app.crypto import encrypt
from app.database import Base
from app.models import Company

ROOT = Path(__file__).resolve().parents[1]


def _load_migrate():
    spec = importlib.util.spec_from_file_location(
        "migrate", ROOT / "scripts" / "migrate_sqlite_to_postgres.py")
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    return mod


migrate = _load_migrate()


def test_naive_datetimes_are_marked_utc():
    """SQLite 讀出來的 datetime 是 naive 的。直接塞進 PostgreSQL 的
    timestamptz,PG 會拿伺服器時區去解讀 —— 容器裡是 UTC、筆電是 UTC+8,
    整批差八小時,而且不會有任何錯誤訊息。"""
    naive = datetime(2026, 9, 22, 1, 30, 0)
    assert migrate.as_utc(naive) == datetime(2026, 9, 22, 1, 30, tzinfo=timezone.utc)


def test_aware_datetimes_are_left_alone():
    aware = datetime(2026, 9, 22, 1, 30, tzinfo=timezone.utc)
    assert migrate.as_utc(aware) is aware


def test_non_datetime_values_pass_through():
    assert migrate.as_utc("Ubot0001") == "Ubot0001"
    assert migrate.as_utc(None) is None
    assert migrate.as_utc(b"\x01\x02") == b"\x01\x02"


def _seed_source(url: str) -> None:
    engine = create_engine(url, future=True)
    Base.metadata.create_all(engine)
    with engine.begin() as conn:
        conn.execute(Base.metadata.tables["companies"].insert(), {
            "id": "11111111-1111-1111-1111-111111111111",
            "slug": "bistro", "name": "微醺之夜", "industry": "restaurant",
            "line_channel_id": None,
            "line_channel_secret_enc": encrypt("chan-secret"),
            "line_channel_token_enc": encrypt("chan-token"),
            "line_destination": "Ubot0001",
            "system_prompt": "你是微醺之夜的客服", "tone": "專業親切",
            "forbidden_phrases": [], "fallback_message": "再問一次",
            "human_mode_timeout_minutes": 30, "vector_collection": "kb_bistro",
            "is_active": True,
            "created_at": datetime(2026, 9, 21, 3, 0, 0),
            "updated_at": datetime(2026, 9, 21, 3, 0, 0),
        })
    engine.dispose()


def test_copies_rows_with_the_ciphertext_untouched(tmp_path):
    """密文照搬不解密。解密再加密等於讓明文金鑰多在記憶體裡出現一次,
    沒有任何好處 —— 而且 FERNET_KEY 不變的話密文本來就還解得開。

    這個測試刻意搬 SQLite → SQLite,只驗「列有沒有完整搬過去」。
    時區正確性由上面那三個 as_utc 的單元測試負責 —— SQLAlchemy 的
    SQLite 方言寫入時會把 tzinfo 丟掉,在這裡斷言時區只會驗到 SQLite
    的行為,驗不到我們的。不要因為這裡沒斷言時區就以為漏測了。
    """
    src = f"sqlite:///{tmp_path / 'source.db'}"
    dst = f"sqlite:///{tmp_path / 'target.db'}"
    _seed_source(src)

    target = create_engine(dst, future=True)
    Base.metadata.create_all(target)   # 模擬 Alembic 已經建好表

    counts = migrate.copy_all(src, dst)
    assert counts["companies"] == 1

    with target.connect() as conn:
        row = conn.execute(select(Company.__table__)).one()
    assert row.slug == "bistro"
    from app.crypto import decrypt
    assert decrypt(row.line_channel_secret_enc) == "chan-secret"


def test_refuses_when_the_target_has_no_tables(tmp_path):
    """建表是 Alembic 的職責。讓這支腳本也能建,就是同一件事有兩個
    真相來源 —— 兩邊總有一天不一致,而且會在「搬完之後某個欄位
    莫名不存在」的時候才發現。"""
    src = f"sqlite:///{tmp_path / 'source.db'}"
    dst = f"sqlite:///{tmp_path / 'empty.db'}"
    _seed_source(src)

    with pytest.raises(SystemExit) as exc:
        migrate.copy_all(src, dst)
    assert "alembic" in str(exc.value).lower()


def test_refuses_to_overwrite_a_non_empty_target_without_force(tmp_path):
    src = f"sqlite:///{tmp_path / 'source.db'}"
    dst = f"sqlite:///{tmp_path / 'target.db'}"
    _seed_source(src)
    _seed_source(dst)   # 目標也有資料

    with pytest.raises(SystemExit) as exc:
        migrate.copy_all(src, dst)
    assert "--force" in str(exc.value)


def test_dry_run_reports_counts_without_writing(tmp_path):
    src = f"sqlite:///{tmp_path / 'source.db'}"
    dst = f"sqlite:///{tmp_path / 'target.db'}"
    _seed_source(src)
    target = create_engine(dst, future=True)
    Base.metadata.create_all(target)

    counts = migrate.copy_all(src, dst, dry_run=True)
    assert counts["companies"] == 1

    with target.connect() as conn:
        assert conn.execute(select(Company.__table__)).all() == []
```

- [ ] **Step 2: 跑測試確認失敗**

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_migrate.py -v
```

Expected: FAIL —— `FileNotFoundError`,`scripts/migrate_sqlite_to_postgres.py` 還不存在。

- [ ] **Step 3: 寫 `scripts/migrate_sqlite_to_postgres.py`**

```python
"""把 SQLite 的資料搬進 PostgreSQL。一次性,跑完就不再用。

    python scripts/migrate_sqlite_to_postgres.py --dry-run
    python scripts/migrate_sqlite_to_postgres.py

前置:目標資料庫的表必須已經由 Alembic 建好 —— 先 `docker compose up -d`,
app 容器的 entrypoint 會跑 `alembic upgrade head`。

這支腳本不建表。建表是 Alembic 的職責,讓第二個地方也能建就是同一件事
有兩個真相來源,兩邊總有一天會不一致,而且會在「搬完之後某個欄位莫名
不存在」的時候才發現。
"""

from __future__ import annotations

import argparse
import sys
from datetime import datetime, timezone
from pathlib import Path

from sqlalchemy import create_engine, func, inspect, select

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from app.database import Base  # noqa: E402
import app.models  # noqa: E402,F401  ← 不 import 的話 metadata 是空的

# 依外鍵順序。companies 一定要先(users 與 knowledge_documents 指向它),
# chat_histories 又指向 users。反過來搬會撞外鍵。
TABLE_ORDER = ("companies", "users", "chat_histories", "knowledge_documents")


def as_utc(value):
    """把 naive datetime 明確標記成 UTC。

    SQLite 不真的保存時區,讀出來是 naive 的。直接塞進 PostgreSQL 的
    timestamptz,PG 會拿「伺服器時區」去解讀 —— 容器裡是 UTC、筆電是
    UTC+8,整批時間默默差八小時,而且不會有任何錯誤訊息。

    標成 UTC 是對的、不是猜的:v1 決策 8 已經確立「時間一律存 UTC」,
    而 SQLite 的 CURRENT_TIMESTAMP 定義本來就是 UTC。
    """
    if isinstance(value, datetime) and value.tzinfo is None:
        return value.replace(tzinfo=timezone.utc)
    return value


def copy_all(source_url: str, target_url: str, *,
             dry_run: bool = False, force: bool = False) -> dict[str, int]:
    """把四張表從來源搬到目標,回傳每張表搬了幾列。"""
    src = create_engine(source_url, future=True)
    dst = create_engine(target_url, future=True)

    try:
        inspector = inspect(dst)
        missing = [t for t in TABLE_ORDER if not inspector.has_table(t)]
        if missing:
            raise SystemExit(
                f"目標資料庫缺少這些表:{'、'.join(missing)}。\n"
                "這支腳本不建表。先跑 `docker compose up -d`,app 容器開機時的 "
                "`alembic upgrade head` 會把表建出來,再跑這支。")

        counts: dict[str, int] = {}
        with src.connect() as s, dst.begin() as d:
            # 先全部檢查完再寫任何一筆。寫到一半才發現第三張表不能覆寫,
            # 留下來的是「搬了一半」的資料庫,比完全沒搬更難處理。
            for name in TABLE_ORDER:
                table = Base.metadata.tables[name]
                existing = d.execute(
                    select(func.count()).select_from(table)).scalar_one()
                if existing and not force:
                    raise SystemExit(
                        f"目標的 {name} 已經有 {existing} 列,拒絕覆寫。"
                        "確定要清掉重搬請加 --force。")

            if force and not dry_run:
                # 反序刪除,不然會撞外鍵
                for name in reversed(TABLE_ORDER):
                    d.execute(Base.metadata.tables[name].delete())

            for name in TABLE_ORDER:
                table = Base.metadata.tables[name]
                rows = [dict(r._mapping) for r in s.execute(select(table))]
                counts[name] = len(rows)
                if dry_run or not rows:
                    continue
                d.execute(table.insert(), [
                    {k: as_utc(v) for k, v in row.items()} for row in rows
                ])
        return counts
    finally:
        src.dispose()
        dst.dispose()


def main(argv=None) -> int:
    parser = argparse.ArgumentParser(prog="migrate_sqlite_to_postgres")
    parser.add_argument("--source", default="sqlite:///./helpdesk.db")
    parser.add_argument(
        "--target", required=True,
        help="例如 postgresql+psycopg://helpdesk:<密碼>@localhost:5432/helpdesk")
    parser.add_argument("--dry-run", action="store_true",
                        help="只印出每張表幾列,不寫入任何東西。")
    parser.add_argument("--force", action="store_true",
                        help="目標已有資料時,先清空再搬。")
    args = parser.parse_args(argv)

    counts = copy_all(args.source, args.target,
                      dry_run=args.dry_run, force=args.force)

    label = "(dry-run,沒有寫入)" if args.dry_run else ""
    print(f"搬遷結果{label}:")
    for name in TABLE_ORDER:
        print(f"  {name:22} {counts[name]:>5} 列")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: 跑測試確認通過**

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_migrate.py -v
```

Expected: 7 passed

- [ ] **Step 5: 先 dry-run 看真實資料有多少**

```powershell
$pw = ((Get-Content .env | Where-Object { $_ -match '^POSTGRES_PASSWORD=' }) -split '=', 2)[1].Trim()
if (-not $pw) { throw ".env 裡沒有 POSTGRES_PASSWORD,回去做 Task 4 Step 2" }
.\.venv\Scripts\python.exe scripts/migrate_sqlite_to_postgres.py --target "postgresql+psycopg://helpdesk:$pw@localhost:5432/helpdesk" --dry-run
```

Expected: 印出四張表各幾列,`companies` 應該是 1。

- [ ] **Step 6: 真的搬**

```powershell
.\.venv\Scripts\python.exe scripts/migrate_sqlite_to_postgres.py --target "postgresql+psycopg://helpdesk:$pw@localhost:5432/helpdesk"
```

- [ ] **Step 7: 驗收標準 4 —— 憑證解得開、時間沒位移**

先把驗證腳本寫進暫存檔(一行式的 `python -c` 在 PowerShell 裡引號很容易爆掉):

```powershell
@'
from sqlalchemy import select
from app.crypto import decrypt
from app.database import session_scope
from app.models import Company

with session_scope() as db:
    c = db.scalar(select(Company))
    print("slug =", c.slug)
    print("token 解得開,長度 =", len(decrypt(c.line_channel_token_enc)))
    print("created_at =", c.created_at)
'@ | Out-File -FilePath verify_migration.py -Encoding utf8

docker compose cp verify_migration.py app:/app/verify_migration.py
docker compose exec app python verify_migration.py
Remove-Item verify_migration.py
```

Expected:
- `slug= bistro`
- token 長度約 172(**解得開就代表 FERNET_KEY 與密文都完好**)
- `created_at` 帶 `+00:00`,而且時間跟 SQLite 裡的一致(不是差八小時)

- [ ] **Step 8: 跑全套測試並 commit**

```powershell
.\.venv\Scripts\python.exe -m pytest -q
git add scripts/migrate_sqlite_to_postgres.py tests/test_migrate.py
git commit -m "feat(migrate): SQLite 搬進 PostgreSQL

主鍵是 UUID 字串不是自增整數(v1 決策 8),所以沒有 sequence 要修 ——
自增整數搬過去序號還停在 1,資料看起來全在,一寫新資料就撞主鍵。

真正的坑是時區:SQLite 讀出來是 naive datetime,直接塞進 timestamptz
會被 PG 拿伺服器時區解讀,整批差八小時而且不報錯。讀出來明確標 UTC,
並留一條回歸測試。

密文照搬不解密。腳本不建表,目標缺表就報錯並告訴人先跑 compose。
檢查全部做完才寫第一筆 —— 寫到一半失敗留下的是搬了一半的資料庫,
比完全沒搬更難處理。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 6: Tailscale 服務與固定網址

**Files:**
- Create: `deploy/funnel.json`
- Modify: `compose.yaml`

**Interfaces:**
- Consumes: Task 4 的 `app` 服務(healthcheck 要能回 ok)
- Produces: 對外的固定網址 `https://helpdesk.<tailnet>.ts.net`

**這個 task 有要人手動做的前置步驟(Step 1-3),不能由程式代勞。**

- [ ] **Step 1: 註冊 Tailscale 並決定 tailnet 名字**

到 https://login.tailscale.com/ 用 Google 或 GitHub 登入(免費,不用信用卡)。

登入後在 admin console 看 **DNS** 頁面,記下 tailnet 名字(形如 `tail9a2f.ts.net`)。

**現在就決定要不要改名。tailnet 名字是網址的一部分 —— 之後改名,填進 LINE Console 的網址就失效了。**

- [ ] **Step 2: 政策檔加 tag 與 funnel 屬性**

admin console → **Access controls**,在 JSON 裡加這兩段:

```json
"tagOwners": { "tag:helpdesk": ["autogroup:admin"] },
"nodeAttrs": [{ "target": ["tag:helpdesk"], "attr": ["funnel"] }]
```

`nodeAttrs` 若已存在就把那個物件加進既有陣列,不要蓋掉整個欄位。

**這一步不做的話 Funnel 是靜默失效的** —— 容器起得來、節點也上線,就是外面連不進去,而且沒有任何錯誤訊息指向這裡。用 CLI 開 Funnel 時 Tailscale 會自動幫你加這段,但我們走 `TS_SERVE_CONFIG` 宣告式設定,不經過 CLI 互動流程。

- [ ] **Step 3: 產一把帶 tag 的 auth key 並填進 `.env`**

admin console → **Settings** → **Keys** → **Generate auth key**:

- **Reusable**:開(容器重建時不必重產)
- **Tags**:選 `tag:helpdesk` ← **這個不能漏**

把 key 填進 `.env` 的 `TS_AUTHKEY=`。

> 打 tag 的理由:Tailscale 節點金鑰預設 **180 天**到期,過期節點離線、Funnel 跟著死 —— 而這會在你完全沒改任何東西的某一天發生。**標記過的節點不適用金鑰到期**,一勞永逸,比手動去 admin console 按 Disable Key Expiry 可靠。

- [ ] **Step 4: 寫 `deploy/funnel.json`**

```json
{
  "TCP": { "443": { "HTTPS": true } },
  "Web": {
    "${TS_CERT_DOMAIN}:443": {
      "Handlers": { "/": { "Proxy": "http://app:8000" } }
    }
  },
  "AllowFunnel": { "${TS_CERT_DOMAIN}:443": true }
}
```

`${TS_CERT_DOMAIN}` 由 Tailscale 自己展開成該節點的 FQDN,不用我們填。

用宣告式設定而不是每次重啟後手動下 `tailscale funnel` —— 後者總有一天會忘,而忘記的症狀是「服務好好的,外面就是連不進去」。

- [ ] **Step 5: `compose.yaml` 加 `tailscale` 服務**

在 `app` 之後、`volumes:` 之前加:

```yaml
  tailscale:
    image: tailscale/tailscale:latest
    # 這個名字是網址的前半段:https://helpdesk.<tailnet>.ts.net
    hostname: helpdesk
    environment:
      TS_AUTHKEY: ${TS_AUTHKEY:?請在 .env 設 TS_AUTHKEY(必須帶 tag:helpdesk)}
      TS_STATE_DIR: /var/lib/tailscale
      TS_SERVE_CONFIG: /config/funnel.json
      # 標記過的節點不適用金鑰到期。沒有這行,180 天後某一天服務會自己
      # 斷線,而且那天你什麼都沒改,完全不會往這裡想。
      TS_EXTRA_ARGS: --advertise-tags=tag:helpdesk
      # 先試 userspace(零特權)。不通再照 Step 7 換 kernel 模式。
      TS_USERSPACE: "true"
    volumes:
      # 這個 volume 是網址的命脈。沒掛的話容器一重建就當成新機器註冊,
      # 而舊節點還在 tailnet 裡佔著 helpdesk 這個名字,新的會拿到
      # helpdesk-1 —— 網址變了,LINE 那邊還指著舊的,症狀是訊息傳出去
      # 沒人回,而 Console 看起來一切正常。
      - ts-state:/var/lib/tailscale
      - ./deploy/funnel.json:/config/funnel.json:ro
    depends_on:
      app:
        condition: service_healthy
    restart: unless-stopped
```

`volumes:` 區塊加上 `ts-state:`:

```yaml
volumes:
  pgdata:
  ts-state:
```

- [ ] **Step 6: 起起來看節點有沒有上線**

```powershell
docker compose up -d
docker compose logs tailscale --tail 40
docker compose exec tailscale tailscale status
```

Expected: `tailscale status` 印出這個節點,名字是 `helpdesk`。

- [ ] **Step 7: userspace 不通時才做 —— 換 kernel 模式**

若 Step 6 的 log 出現 TUN 相關錯誤,或 Step 8 連不進去,把 `tailscale` 服務改成:

```yaml
      TS_USERSPACE: "false"
    cap_add:
      - net_admin
    devices:
      - /dev/net/tun:/dev/net/tun
```

(`TS_USERSPACE` 那行換掉,`cap_add` 與 `devices` 加在 `volumes` 同層。)

然後 `docker compose up -d --force-recreate tailscale` 重試 Step 6。

**兩條路都寫在這裡,是因為官方文件沒有白紙黑字寫 userspace 支援 Funnel** —— 它說 userspace「works everywhere」、只有 kernel 模式需要那兩項,但沒有針對 Funnel 表態。先試零特權的,不行再退。

- [ ] **Step 8: 驗收標準 3 —— 從外網打得到**

```powershell
docker compose exec tailscale tailscale status --json | ConvertFrom-Json | Select-Object -ExpandProperty Self | Select-Object DNSName
```

記下 FQDN(形如 `helpdesk.tail9a2f.ts.net.`,去掉結尾的點)。

```powershell
curl.exe -s https://helpdesk.tail9a2f.ts.net/health
```

Expected: `{"status":"ok","version":"0.1.0","database":"ok"}`

**用手機關掉 Wi-Fi、走行動網路再打一次同一個網址** —— 這才真的證明它從外網進得來,而不是走了本機的捷徑。

- [ ] **Step 9: 驗收標準 6 —— 網址真的固定(這是整段的核心)**

```powershell
docker compose down
docker compose up -d
docker compose exec tailscale tailscale status --json | ConvertFrom-Json | Select-Object -ExpandProperty Self | Select-Object DNSName
```

Expected: **跟 Step 8 記下的完全一樣。**

不一樣(例如變成 `helpdesk-1`)就是 `ts-state` volume 沒生效,**停下來修好再往下** —— 網址會變的話,前面所有工作等於沒做。

- [ ] **Step 10: Commit**

```powershell
git add deploy/funnel.json compose.yaml
git commit -m "feat(tunnel): Tailscale Funnel 提供固定 HTTPS 網址

Cloudflare Named Tunnel 要自有網域,免費前提下出局;Quick Tunnel 每次
重啟換網址。Funnel 免費、憑證自動,而且網址固定。

三個會讓它悄悄失效的地方都處理了:
- ts-state volume 掛住節點身分,否則重建會變成 helpdesk-1
- --advertise-tags 讓節點不適用 180 天金鑰到期
- funnel.json 用宣告式設定,不靠人每次重啟後下指令

政策檔的 nodeAttrs 要人手動加(CLI 會自動加,宣告式設定不會)——
漏了的話 Funnel 是靜默失效,沒有任何錯誤訊息指向那裡。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 7: `app.cli set-webhook`

**Files:**
- Modify: `app/line/client.py`
- Modify: `app/cli.py`
- Modify: `scripts/dev.py`
- Test: `tests/test_cli_set_webhook.py`(新建)

**Interfaces:**
- Consumes: `app.crypto.decrypt`、`app.database.session_scope`、`app.models.Company`
- Produces: `app.line.client.set_webhook_endpoint(access_token, endpoint, *, timeout=20.0) -> httpx.Response`;`app.cli.run_set_webhook(slug, url) -> None`

網址固定之後,`scripts/dev.py --slug` 那套自動寫回只服務筆電開發流程。compose 這條路需要的是「一次性把固定網址寫進去」,而那一步不該靠人在 Console 手貼。

- [ ] **Step 1: 寫失敗的測試**

新建 `tests/test_cli_set_webhook.py`:

```python
"""app.cli set-webhook 的測試。

這道指令會真的去改別人 LINE 帳號的設定,所以「不該打的時候一個 API
都不能打」跟「打了要成功」一樣重要。
"""

import httpx
import pytest

from app import cli
from app.crypto import encrypt
from app.database import Base, engine, session_scope
from app.models import Company


@pytest.fixture(autouse=True)
def _db():
    Base.metadata.create_all(engine)
    with session_scope() as db:
        db.add(Company(
            slug="bistro", name="微醺之夜", industry="restaurant",
            system_prompt="客服", vector_collection="kb_bistro",
            line_channel_secret_enc=encrypt("chan-secret"),
            line_channel_token_enc=encrypt("chan-token"),
        ))
    yield
    Base.metadata.drop_all(engine)


def test_sends_the_url_with_that_companys_token(monkeypatch):
    seen = {}

    def fake_put(url, json, headers, timeout):
        seen["url"] = url
        seen["endpoint"] = json["endpoint"]
        seen["auth"] = headers["Authorization"]
        return httpx.Response(200, request=httpx.Request("PUT", url))

    monkeypatch.setattr(httpx, "put", fake_put)
    cli.run_set_webhook("bistro", "https://helpdesk.tail9a2f.ts.net")

    assert seen["endpoint"] == "https://helpdesk.tail9a2f.ts.net/webhook/bistro"
    assert seen["auth"] == "Bearer chan-token"


def test_unknown_slug_never_touches_line(monkeypatch):
    """找不到公司就一個 API 都不能打 —— 拿不到 token 的情況下還去打,
    等於用別人的憑證或空 token 去改設定。"""
    called = []
    monkeypatch.setattr(httpx, "put", lambda *a, **kw: called.append(a))

    with pytest.raises(SystemExit) as exc:
        cli.run_set_webhook("nope", "https://x.ts.net")

    assert called == []
    assert "nope" in str(exc.value)


def test_line_rejecting_the_url_is_a_hard_failure(monkeypatch):
    """LINE 回非 200 要讓指令失敗。印一句話就當沒事的話,人會以為
    設定好了,而症狀要到 demo 現場傳訊息沒回應才出現。"""
    monkeypatch.setattr(httpx, "put", lambda url, json, headers, timeout:
                        httpx.Response(400, text="Invalid webhook endpoint URL",
                                       request=httpx.Request("PUT", url)))

    with pytest.raises(SystemExit) as exc:
        cli.run_set_webhook("bistro", "https://x.ts.net")

    assert "400" in str(exc.value)


def test_trailing_slash_does_not_produce_a_double_slash(monkeypatch):
    """貼網址時很容易多帶一個斜線。//webhook/bistro 在 LINE 那邊會
    Verify 失敗,而錯誤訊息只說網址無效。"""
    seen = {}
    monkeypatch.setattr(httpx, "put", lambda url, json, headers, timeout:
                        seen.update(endpoint=json["endpoint"]) or
                        httpx.Response(200, request=httpx.Request("PUT", url)))

    cli.run_set_webhook("bistro", "https://helpdesk.tail9a2f.ts.net/")
    assert seen["endpoint"] == "https://helpdesk.tail9a2f.ts.net/webhook/bistro"
```

- [ ] **Step 2: 跑測試確認失敗**

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_cli_set_webhook.py -v
```

Expected: FAIL —— `AttributeError: module 'app.cli' has no attribute 'run_set_webhook'`

- [ ] **Step 3: `app/line/client.py` 加共用的 PUT**

在 `SUFFIX = "(訊息過長已截斷)"` 那行之後加:

```python
WEBHOOK_ENDPOINT_PATH = "/v2/bot/channel/webhook/endpoint"


def set_webhook_endpoint(access_token: str, endpoint: str,
                         *, timeout: float = 20.0) -> httpx.Response:
    """把 webhook 網址寫回 LINE Console。

    回傳原始 Response,不自己判斷成敗 —— 兩個呼叫端的需求不同:
    scripts/dev.py 失敗只印一句話(自動更新是便利功能,不該讓 tunnel
    監看整個停掉),CLI 失敗要回非零離開碼(人是特地來設定的,
    沒設成功卻靜悄悄比較糟)。在這裡就決定的話,總有一邊是錯的。
    """
    return httpx.put(
        f"{API_BASE}{WEBHOOK_ENDPOINT_PATH}",
        json={"endpoint": endpoint},
        headers={"Authorization": f"Bearer {access_token.strip()}"},
        timeout=timeout,
    )
```

- [ ] **Step 4: `app/cli.py` 加 `run_set_webhook`**

在 `run_seed` 之後、`_ensure_utf8_stdout` 之前加:

```python
def run_set_webhook(slug: str, url: str) -> None:
    """把固定網址一次性寫進 LINE Console。

    網址固定之後這道指令一輩子只跑一次 —— 但那一次如果是人在 Console
    手貼,就有貼錯、漏按 Verify、多一個斜線的機會,而這些的症狀都是
    「訊息傳出去沒人回」,完全指不到真正原因。
    """
    # 只有真的要連資料庫時才 import(理由見檔頭的 import 註解)
    from sqlalchemy import select

    from app.crypto import decrypt
    from app.database import session_scope
    from app.line.client import set_webhook_endpoint
    from app.models import Company

    # 多一個斜線就變成 //webhook/<slug>,LINE 那邊 Verify 會失敗,
    # 而錯誤訊息只說網址無效。
    endpoint = f"{url.strip().rstrip('/')}/webhook/{slug}"

    with session_scope() as db:
        company = db.scalar(select(Company).where(Company.slug == slug))
        if company is None:
            raise SystemExit(
                f"找不到 slug「{slug}」。先跑一次 `python -m app.cli seed`,"
                "或用 `python -m app.cli list` 看有哪些行業。")
        token = decrypt(company.line_channel_token_enc)

    r = set_webhook_endpoint(token, endpoint)
    if r.status_code != 200:
        raise SystemExit(
            f"LINE 拒絕了這個網址(HTTP {r.status_code}):{r.text[:300]}\n"
            "常見原因:網址還沒真的通(LINE 會實際去打它驗證),"
            "或 tunnel 還沒起來。先 curl 一下那個網址的 /health 再重試。")
    print(f"✓ webhook 已設成 {endpoint}")
    print("  不必再開 Console、也不必按 Verify。")
```

在 `main()` 的 `sub.add_parser("list")` 之後加:

```python
    w = sub.add_parser("set-webhook")
    w.add_argument("--slug", required=True)
    w.add_argument("--url", required=True,
                   help="服務的根網址,例如 https://helpdesk.tail9a2f.ts.net")
```

在 `main()` 的 `if args.cmd == "validate":` 之後加:

```python
    if args.cmd == "set-webhook":
        run_set_webhook(args.slug, args.url)
        return 0
```

- [ ] **Step 5: `scripts/dev.py` 改用共用函式**

把 `_register_webhook()` 裡的這段:

```python
    r = httpx.put(LINE_ENDPOINT_API, json={"endpoint": webhook_url},
                  headers={"Authorization": f"Bearer {token}"}, timeout=20)
```

換成:

```python
    from app.line.client import set_webhook_endpoint
    r = set_webhook_endpoint(token, webhook_url)
```

並把檔案上方的 `LINE_ENDPOINT_API = "https://api.line.me/v2/bot/channel/webhook/endpoint"` 刪掉(改由 `app.line.client` 持有)。

- [ ] **Step 6: 跑測試**

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_cli_set_webhook.py tests/test_dev_script.py -v
```

Expected: 全部 passed —— `test_dev_script.py` 的既有測試不能因為這次抽取而變紅。

- [ ] **Step 7: 對真的 LINE 帳號跑一次(驗收標準 5 的前置)**

```powershell
docker compose exec app python -m app.cli set-webhook --slug bistro --url https://helpdesk.tail9a2f.ts.net
```

Expected: `✓ webhook 已設成 https://helpdesk.tail9a2f.ts.net/webhook/bistro`

- [ ] **Step 8: 驗收標準 5 —— 手機實測整條鏈**

用手機對官方帳號依序傳:

| # | 傳什麼 | 預期 |
|---|---|---|
| 1 | `有停車位嗎` | 門口兩格 + 對面收費停車場 |
| 2 | `那可以帶狗嗎` | 戶外可以、室內不行,**而且看得出它記得在講同一家店** |
| 3 | (傳一張內建貼圖) | 「我目前只看得懂文字訊息」 |

第 2 則是關鍵 —— 它證明短期記憶跨訊息成立,而那份記憶現在存在 PostgreSQL 裡。

- [ ] **Step 9: 跑全套並 commit**

```powershell
.\.venv\Scripts\python.exe -m pytest -q
git add app/line/client.py app/cli.py scripts/dev.py tests/test_cli_set_webhook.py
git commit -m "feat(cli): set-webhook 把固定網址一次性寫進 LINE Console

網址固定之後這道指令一輩子只跑一次,但那一次若是人在 Console 手貼,
就有貼錯、漏按 Verify、多一個斜線的機會 —— 三種的症狀都是
「訊息傳出去沒人回」,完全指不到真正原因。

PUT 抽到 app.line.client 給 dev.py 與 CLI 共用,但刻意回傳原始
Response 不自己判斷成敗:dev.py 失敗只印一句話(自動更新是便利功能,
不該讓 tunnel 監看整個停掉),CLI 失敗要回非零離開碼(人是特地來
設定的)。在共用層就決定的話,總有一邊是錯的。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 8: 自動重啟驗證與文件更新

**Files:**
- Modify: `README.md`
- Modify: `docs/demo-checklist.md`
- Modify: `docs/report.md`

**Interfaces:**
- Consumes: 前七個 task 的全部產出
- Produces: 無程式介面。產出是**驗收標準 7、8 通過的證據**,以及文件與現實一致。

- [ ] **Step 1: 驗收標準 7 —— 殺掉 app 容器會自己回來**

```powershell
docker kill (docker compose ps -q app)
Start-Sleep -Seconds 10
docker compose ps app
curl.exe -s http://127.0.0.1:8000/health
```

Expected: `app` 又是 `running`,`/health` 回 ok。

- [ ] **Step 2: Docker Desktop 設成開機啟動**

Docker Desktop → 齒輪 **Settings** → **General** → 勾 **Start Docker Desktop when you sign in to your computer**。

**這一步不做,`restart: unless-stopped` 在重開機後等於沒有** —— 而平常測試完全看不出來,因為容器本來就在跑。

- [ ] **Step 3: 驗收標準 8 —— 重開機(這一步要真的重開)**

```powershell
Restart-Computer
```

開機後登入,等 Docker Desktop 起來,然後:

```powershell
cd C:\Users\User\Desktop\universal-ai-helpdesk
docker compose ps
curl.exe -s https://helpdesk.tail9a2f.ts.net/health
docker compose exec tailscale tailscale status --json | ConvertFrom-Json | Select-Object -ExpandProperty Self | Select-Object DNSName
```

Expected:
- 三個容器都在跑,**沒有手動下過任何指令**
- 外網網址回 ok
- **FQDN 跟 Task 6 Step 8 記下的完全一樣**

FQDN 變了就代表 `ts-state` volume 在重開機後沒被掛回去,**停下來修好** —— 這是驗收標準 8 真正在驗的東西。

- [ ] **Step 4: `README.md` 的「怎麼跑起來」改成 compose**

把既有的「怎麼跑起來」一節整段換成:

````markdown
## 怎麼跑起來

```bash
cp .env.example .env    # 填 FERNET_KEY、OPENROUTER_API_KEY、POSTGRES_PASSWORD、TS_AUTHKEY
docker compose up -d
curl http://127.0.0.1:8000/health
```

三個容器:`app`(FastAPI,開機自動跑 Alembic)、`db`(PostgreSQL)、
`tailscale`(提供固定的對外 HTTPS 網址)。

第一次還要做的事、以及 Tailscale 那邊要設的兩段政策,寫在
[部署設計文件第十一節](docs/superpowers/specs/2026-09-22-deploy-design.md)。

把固定網址寫進 LINE:

```bash
docker compose exec app python -m app.cli set-webhook --slug bistro --url https://<你的網址>
```

**這道指令一輩子只要跑一次。** 網址是固定的,重啟、重開機都不會變。

> **`FERNET_KEY` 請自己留一份備份。** 它一弄丟,資料庫裡所有 LINE 憑證
> 就解不開了,只能回 LINE Console 重拿。以前 `helpdesk.db` 躺在桌面隨手
> 可以備份,現在資料在 Docker volume 裡,比較不容易注意到這件事。

筆電開發流程(SQLite + Quick Tunnel)仍然可用:`python scripts/dev.py --slug bistro`。
````

- [ ] **Step 5: `README.md` 的「狀態」與「下一步」更新**

「狀態」一節開頭那句改成:

```markdown
**v1 完成,已經接在真的 LINE 官方帳號上跑過;B 階段部署完成 —— 服務跑在
容器裡、資料在 PostgreSQL、對外是固定網址,當掉與重開機都會自己回來。**
145 個自動測試全綠,SQLite 與 PostgreSQL 各跑一輪。
```

「下一步」表格把第一列換成:

```markdown
| **速率限制** | `/webhook/{slug}` 是公開端點。簽章擋得住偽造,擋不住「有人拿真的帳號狂傳」—— 每則都會打 LLM,免費額度一分鐘燒光,之後所有客人都收到 fallback。部署做完了,這是「能放著跑」剩下的最後一塊 |
```

- [ ] **Step 6: `docs/demo-checklist.md` 拿掉抄網址那幾條**

「現場(面試前十分鐘)」整節換成:

```markdown
## 現場(面試前十分鐘)

- [ ] `docker compose ps` —— 三個都在跑
- [ ] `curl https://<你的固定網址>/health` 回 `{"status":"ok",...,"database":"ok"}`
- [ ] **自己先傳一則訊息**,確認有回 —— 不要讓面試官當第一個測試者

**網址是固定的,不用抄、不用貼、不用按 Verify。** 電腦重開過也一樣。

會議室換 Wi-Fi 不影響 —— Tailscale 會自己重連。真的連不上時,
`docker compose restart tailscale`。
```

「第一次串接才要做的」一節,把 Webhook URL 與 Verify 那兩條換成:

```markdown
- [ ] `docker compose exec app python -m app.cli set-webhook --slug bistro --url https://<你的固定網址>`
      —— 一輩子只跑一次
```

其餘三條(**Use webhook**、**Webhook redelivery**、關掉 **Auto-reply / Greeting**)照舊保留。

「如果當場壞掉」的第 2 條換成:

```markdown
2. LINE 說 **530**?→ 現在網址是固定的,所以 530 不再是「網址過期」,
   而是服務或 tunnel 沒起來。`docker compose ps` 看哪個不在,
   `docker compose logs <服務名> --tail 50` 看原因
```

- [ ] **Step 7: `docs/report.md` 第八節限制 1 改寫**

把限制 1 整條換成:

```markdown
1. ~~沒有部署~~ → **已部署,但仍在自有硬體上。** 服務、PostgreSQL 與
   tunnel 都跑在容器裡,固定網址、當掉與重開機都會自己回來(B 階段)。
   剩下的缺口有兩個:**電腦沒開就連不到**(沒有上雲),以及
   **「卡住但沒退出」不會自動處理** —— `restart: unless-stopped` 只在
   容器退出時作用,而 Docker 的 healthcheck 不會重啟容器(那是 Swarm)。
   補後者要多掛一個 autoheal sidecar,列在下一步。
```

第九節「下一步」的第 1 列換成:

```markdown
| 1 | **速率限制 + autoheal** | 部署做完之後,「能放著跑」只剩這兩塊。速率限制是限制 2;autoheal 補的是上面限制 1 留下的「卡住但沒退出」 |
```

- [ ] **Step 8: 全套測試最後跑一次並 commit**

第一輪 SQLite:

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

第二輪 PostgreSQL。**用臨時容器,不要用正式資料庫** —— 測試會建表又 `drop_all`,
拿剛搬好的資料去跑等於把它刪掉:

```powershell
docker run --rm -d --name helpdesk-pgtest -e POSTGRES_PASSWORD=test -e POSTGRES_DB=helpdesk_test -p 55432:5432 postgres:17-alpine
Start-Sleep -Seconds 5
$env:DATABASE_URL = "postgresql+psycopg://postgres:test@localhost:55432/helpdesk_test"
.\.venv\Scripts\python.exe -m pytest -q
Remove-Item Env:DATABASE_URL
docker stop helpdesk-pgtest
```

Expected: 兩輪都全綠。

```powershell
git add README.md docs/demo-checklist.md docs/report.md
git commit -m "docs: 狀態改成已部署,檢查清單拿掉抄網址那幾步

demo 前流程從六步變三步 —— 網址固定之後,抄網址、貼 Console、
按 Verify 這三件事整個消失。530 的意義也跟著變了:不再是
「網址過期」,而是服務或 tunnel 沒起來,排查方向完全不同。

報告的限制 1 沒有直接畫掉,改寫成「已部署但仍在自有硬體上」,
並列出留下的兩個缺口:電腦沒開就連不到、卡住但沒退出不會自動處理。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## 完成後的狀態

對照 [spec 第十節驗收標準](../specs/2026-09-22-deploy-design.md):

| # | 驗收項目 | 由哪裡保證 |
|---|---|---|
| 1 | 三個容器起得來 | Task 4 Step 4、Task 6 Step 6 |
| 2 | 內部通 | Task 4 Step 5 |
| 3 | 外部通 | Task 6 Step 8(含手機走行動網路實測) |
| 4 | 搬遷正確、憑證解得開、時間沒位移 | Task 5 Step 7 + `test_migrate.py` 的回歸測試 |
| 5 | 整條鏈通 | Task 7 Step 8 |
| 6 | **網址真的固定** | Task 6 Step 9 |
| 7 | 當掉會回來 | Task 8 Step 1 |
| 8 | **重開機會回來,而且網址不變** | Task 8 Step 3 |
| 9 | 換行業照舊 | Task 8 Step 6 的檢查清單(指令改成 `docker compose exec`) |
| 10 | 測試全綠(兩種資料庫) | Task 1 Step 4-5、Task 8 Step 8、CI 的 `test-postgres` job |

## 沒有做、而且是故意的

| 缺口 | 寫在哪 |
|---|---|
| 電腦沒開就連不到(沒上雲) | spec 第一節「刻意的解耦」、report 限制 1 |
| 「卡住但沒退出」不會自動處理 | spec 第五節、report 限制 1 與下一步 |
| 速率限制 | spec 第一節「不做」、report 限制 2 |
